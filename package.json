{
  "name": "wcs-rice-roll",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "飼料稲WCS collection game for iPhone / iPad (WebGL 3D)",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/three": "^0.185.0",
    "typescript": "^5.9.0",
    "vite": "^8.2.1"
  }
}
