{
  "name": "invisible-eye-tuning-room",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "見えない目の調律室――入る人だけ見つけよう",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/three": "^0.168.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "vitest": "^2.1.1"
  }
}
