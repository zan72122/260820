{
  "name": "takenoko-hori",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "たけのこ掘り - a mobile WebGL bamboo-shoot digging game for iPhone / iPad",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "lint": "eslint src tests --ext .js",
    "test:e2e": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "eslint": "^9.15.0",
    "vite": "^8.2.1"
  }
}
