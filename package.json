{
  "name": "one-heart-four-windows",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ひとつの心臓、四つの窓 — a discovery game about how one heartbeat sounds different across four chest listening areas.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "playwright": "^1.62.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
