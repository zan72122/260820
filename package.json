{
  "name": "cassava-harvest",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Realistic 3D cassava harvesting game for young children (mobile web, iPhone/iPad).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4173",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/three": "^0.185.0",
    "typescript": "~5.9.3",
    "vite": "^8.2.2"
  }
}
