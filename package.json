{
  "name": "water-blast",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Water Blast - push the raft up the hill. A toddler-facing causal discovery game.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4173",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "playwright": "1.62.1",
    "typescript": "5.9.3",
    "vite": "8.2.2"
  }
}
