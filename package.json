{
  "name": "moon-pendulum-clock",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "月の振り子時計 - a swing-as-pendulum park clock game for very young children",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --host",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
