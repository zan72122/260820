# FONT LAB NOTES

実装中に確定した、parametric geometry と幼児 UX の決定だけを残す。

## Canonical glyph system

- 全 glyph は `src/glyph/spec.ts` の純関数群が唯一の情報源。視覚 mesh・衝突判定・
  テストは同じ関数を読む。geometry 更新は handle 入力時のみ。
- **O = リングゲージ + 12枚イリス**。文字を水平に置き上から読む構図にした。
  「上から球を落として穴を試験する」が現実の機械(リングゲージ/絞り)と一致し、
  counter と外形を同時に読めるため。stroke 変化は剛体リーフの回転のみで実現
  (重なり板方式)。頂点書き換えゼロ。
  - リーフ形状は解析的に導出: 切断エッジ=円弧(半径 q)、pivot 周り回転角は
    余弦定理で weight から閉形式。γ は鈍角になり得るので law of cosines 必須
    (asin の分枝バグを一度踏んだ)。
  - 剛体リーフが薄 stroke 時に文字外径(0.42)内へ物理的に収まらないことが
    数値検証で判明 → 実物と同じく「文字リムより一段広い機構ドラム」(r0.56-0.575)
    にリーフを収納。文字はリム+リーフ上面のみ、ドラムは鋳鉄色で分離。
  - stroke 範囲は 0.16–0.34(hole r 0.08–0.26)。0.10 まで細くするとリーフの
    旋回域が確保できず、ゲーム的にも rubber の通過閾値が端に寄りすぎる。
- **C = 楕円中心線 sweep 壁(コラル)**。幅 u で Rx・aperture 半角(24°→46°)・
  stroke taper が連動。単純 x-scale ではない。トポロジー固定の SweepWall を
  入力時だけ位置書き換え。terminal block と seam 板は剛体で再ポーズ。
- **I = 積層プレート parallelogram**。slant で stem 剛体プレート群が sin/cos で
  再配置(真の平行四辺形: 高さも cos で沈む)、top bar は平行移動+傾き
  (barTipFactor 0.75)。ball は track 上の 1-DOF 実時間シミュレーション —
  handle に即応するのが第三ステーションの核。

## 球と閾値(成功域は広い帯)

- rubber d0.30(初期球)/ wood d0.13 / steel d0.42。
- O: rubber は weight ≤ ~0.56 で通過、steel は ≤ ~0.22、wood はほぼ常に通過。
- C: rubber は width ≥ ~0.5、wood は常時、steel は ≥ ~0.94。
- I: |slant| > 0.16 で球が detent を離れる。

## 幼児 UX の決定

- 必須操作は一指のみ: 大型 carriage を左右へ / lever を倒す / 引きレバー。
  引きレバーは「タップでも発火」(4歳の引き動作は不安定)。
- 端点は彫刻ミニチュア(細O/太O、狭C/広C、直立I/斜I)。文字・数字・軸名なし。
- 注意誘導は暖色 task light の弱い明滅のみ(赤×・説明テキストなし)。
- blocked の見せ方: 弾む球(rubber/wood)は前方トレイへ転がり出る。重い steel は
  counter の口に「はまって」静止し、その場で handle を細くすると落下する —
  操作→結果の live 因果が最強の教材なので意図的に残した。
- I station は球が track に載った後 1.6 秒静止(予想の間)→ slant lever に light。
- 第三文字は I を採用。A 案(crossbar 高さ)は「傾き→方向」の因果が
  左右対称の即時反応として読める I 案に劣ると判断(A は水位/経路変化が間接的)。
- 自由実験: rack から球を運ぶ(tap でも可)。1 ステーション 1 ハンドルなので
  「一試行一変数」が構造的に保証される。

## モバイル/E2E

- station の球は各1個、scripted path + 1-DOF のみ。カオスなし・端末非依存。
- `?e2e=1` で dpr1/影512。`window.__lab` が論理時間 step と実 screen 座標を公開。
- 回転時は resize のみ(状態は JS 内)→ 軸値・球位置は自動的に保持。

## 実装教訓(保守用)

- THREE.LatheGeometry はプロファイル進行方向で法線が決まる(上向き区間=外向き、
  内向き区間=上向き)。断面ループを「外面は上向きに、床は内向きに」辿らないと
  壁が背面カルリングされ、接線方向に「内部が覗く黒いヒゲ/セイル」として現れる。
  O のドラム・リム・漏斗・ボウルはこの向きで統一した。開放円筒(feeder cup 等)は
  DoubleSide にする。
