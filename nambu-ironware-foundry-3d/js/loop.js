// camera + animation
const clock=new THREE.Clock();function animate(){requestAnimationFrame(animate);const t=clock.getElapsedTime(),dt=Math.min(clock.getDelta?.()||.016,.033);const sc=state.scene;const target=new THREE.Vector3(.2,1.05,-.15);let dist=7.7,y=2.95;if(sc>=1&&sc<=5){target.set(-.05,1.45,.35);dist=7.0;y=3.1}else if(sc>=7&&sc<=11){target.set(.25,1.05,-.2);dist=5.8;y=2.35}else if(sc===12){target.set(.3,1.25,-.1);dist=6.2;y=2.6}y*=settings.camY/100;const yaw=.1+state.yaw;const shake=(state.drag?settings.shake/100*.012:0)*(Math.sin(t*37)+Math.sin(t*23)*.4);camera.position.x=target.x+Math.sin(yaw)*dist+shake;camera.position.z=target.z+Math.cos(yaw)*dist;camera.position.y=lerp(camera.position.y,y+state.pitch*2,.05);camera.lookAt(target);ladle.position.x=lerp(ladle.position.x,state.ladleX,.08);ladle.position.y=lerp(ladle.position.y,state.ladleY,.08);ladle.rotation.z=lerp(ladle.rotation.z,-state.tilt*.95,.09*(1.2-settings.weight/160));workerL.rotation.z=state.tilt*.04;workerR.rotation.z=-state.tilt*.04;state.streamOn=clamp(state.streamOn,0,1);if(sc>=3&&sc<=5)updateStream(t);else if(streamMesh){scene.remove(streamMesh);streamMesh.geometry.dispose();streamMesh=null}
for(const d of droplets){if(d.life>0){d.life-=.016;d.v.y-=3.7*.016;d.m.position.addScaledVector(d.v,.016);d.m.scale.setScalar(clamp(d.life*2,0,1));if(d.life<=0)d.m.visible=false}}for(const d of dust){if(d.life>0){d.life-=.016;d.v.y+=.03*.016;d.m.position.addScaledVector(d.v,.016);d.m.material.opacity=clamp(d.life*.12,0,.18);if(d.life<=0)d.m.visible=false}}
embers.rotation.y=t*.03;fireLight.intensity=50+Math.sin(t*8)*6+Math.sin(t*13)*3;renderer.render(scene,camera)}animate();
addEventListener('resize',()=>{renderer.setSize(innerWidth,innerHeight);camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix()});applyScene(+localStorage.getItem('nambu3d-scene')||0);applySettings();
